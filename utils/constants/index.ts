// Constants
export const SERIES_STATUS: { [key: string]: string } = {
  COMPLETED: 'completed',
  ONGOING: 'ongoing',
}

export const DEFAULT_VALUES: { [key: string]: number } = {
  ZERO: 0,
  AVERAGE_DIVISOR_MIN: 1,
}
